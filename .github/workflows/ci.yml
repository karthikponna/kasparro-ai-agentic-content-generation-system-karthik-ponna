name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"

    - name: Set up Python ${{ matrix.python-version }}
      run: uv python install ${{ matrix.python-version }}

    - name: Install dependencies
      run: uv sync --dev

    - name: Run linting with ruff
      run: |
        uv run ruff check src/ tests/ --exit-zero
        uv run ruff format --check src/ tests/ --exit-zero
      continue-on-error: true

    - name: Run unit tests
      run: uv run pytest tests/unit/ -v --tb=short

    - name: Run golden contract tests
      run: uv run pytest tests/golden/ -v --tb=short

    - name: Run all tests with coverage
      run: uv run pytest tests/ -v --cov=src --cov-report=term-missing --ignore=tests/integration/
      env:
        OPENAI_API_KEY: "test-key-for-unit-tests"

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install 3.11

    - name: Install dependencies
      run: uv sync --dev

    - name: Check import sorting
      run: uv run ruff check --select I src/ tests/
      continue-on-error: true

    - name: Check code style
      run: uv run ruff check src/ tests/
      continue-on-error: true
